# Example environment variables — copy to .env and fill with real values
# Do NOT commit .env with real secrets.

# Database connection (absolute or relative sqlite path)
SQLALCHEMY_DATABASE_URL=sqlite:///C:/full/path/to/MoneyManagement.db

# Secret for JWT or other signing
SECRET_KEY=change_this_to_a_secure_random_string

# JWT algorithm and token config
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=720